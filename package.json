{
  "name": "shuffle-lunch",
  "version": "1.0.0",
  "description": "Shuffle Lunch Matching App",
  "author": "h-oikawa-jp <xyz@h-oikawa.jp>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "clean": "yarn clean:nuxt;yarn clean:dist",
    "clean:nuxt": "rm -rf .nuxt",
    "clean:dist": "rm -rf dist",
    "dist": "yarn clean;yarn generate",
    "serve": "firebase serve --only hosting",
    "deploy": "yarn dist;firebase deploy",
    "deploy:hosting": "yarn dist;firebase deploy --only hosting",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "precommit": "yarn lint"
  },
  "dependencies": {
    "bulma": "^0.7.4",
    "firebase": "^5.8.6",
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "babel-eslint": "^10.0.1",
    "eslint": "^4.19.1",
    "eslint-friendly-formatter": "^4.0.1",
    "eslint-loader": "^2.1.1",
    "eslint-plugin-vue": "^4.0.0",
    "firebase-tools": "^6.4.0",
    "node-sass": "^4.11.0",
    "sass-loader": "^7.1.0"
  }
}
